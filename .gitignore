# Local collaboration file (not part of the public repo)
CLAUDE.md

# Foundry (project lives in contracts/)
cache/
out/
# keep Sepolia broadcast logs (deployment records), ignore local/dry-run
contracts/broadcast/*/31337/
contracts/broadcast/**/dry-run/

# Env / secrets
.env
.env.*

# Frontend / node
node_modules/
frontend/dist/

# OS / editor
.DS_Store
